import './bootstrap';

// Builder (resources/js/builder.js) is loaded separately via Vite for pages that need it.
// Use @vite('resources/js/builder.js') in Blade templates to include the GrapesJS page builder.
